# Cloudflare Pages _redirects file
# Used for URL redirects and rewrites

# Redirect non-www to www (optional)
# http://mastoholdings.com/* https://www.mastoholdings.com/:splat 301!

# Security headers and trailing slash handling
/*   /index.html  200

# Old URLs to new ones (add as needed)
# /old-page /new-page 301
